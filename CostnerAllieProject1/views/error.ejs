<%- include('./partials/header') %>
  <h1>Error!</h1>
  <p>Status: <%= status %></p>
  <p><%= message %></p>

  <% if (process.env.NODE_ENV !== 'production' && error && error.stack) { %>
    <pre><%= error.stack %></pre>
  <% } %>

<%- include('./partials/footer') %>